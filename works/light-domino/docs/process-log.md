# 光のドミノ ─ 制作記録

## Phase 0: アイデア具体化

- お題「光のドミノの道を描いて、ひと押しで倒す」を変えず、操作を「なぞる → 端をタップ」、感情ゴールを「緊張と畏怖」に固定した
- 気持ちよさの核を、連鎖速度だけでなく「最後の直前に 160ms 音を引く → 最後の板・道全体の光・低音付き和音を同時に解放する」という同期で定義した
- 入力の巧拙を要求しないため、生の軌跡ではなく移動平均と弧長再サンプリングで板を配置する設計にした
- 暗い背景で金が濁るのを避けるため、`#FFC857` は不透明な細い芯として 1 回描き、周辺光を別レイヤーに分離した
- 画像素材は使わず、板、床、光跡、終演演出を 2D canvas で描く。画像発注は 0 枚
- `pnpm check` の初回は雛形にサムネイルが無く失敗した。企画の配色と構図を表す手続き生成 SVG を WebP へ変換して `public/thumbnail.webp`（1200×630）を置き、再実行で通過した。このサムネイルは画像生成モデルによる素材ではないため `generatedImages` は空のまま

## Phase 1: 実装

- `docs/architecture.md` を正本に、雛形（波紋作品）から全面書き換えた。状態機械は `intro / tracing / aligned / chain / finale`
- モジュール: `main.ts`（状態機械・入力・描画）/ `tuning.ts`（定数一元）/ `music.ts` + `scale.ts`（C メジャーペンタ、進行率 → C4〜E6 の単調上昇）/ `path.ts`（移動平均・弧長再サンプリング・板配置の純粋関数）/ `chain.ts`（連鎖タイムラインの純粋計算）/ `audio/engine.ts`（契約 + Noop + `computeChainSchedule`）/ `audio/synth-engine.ts`（マリンバ風モーダル合成）/ `audio/audio-tuning.ts`（音響の定数）/ `quality.ts` / `art-hook.ts`（雛形のまま）
- 連鎖の時刻は音側の `beginChain()` が `ChainSchedule` で返し、視覚はその時刻で倒れを描く。Noop（`?mute`・未解錠）も同じ純粋計算を使うため、無音時も連鎖の速さが変わらない
- hush は「最後の板の着地の 160ms 前 → 着地」。transientBus を 80ms で -12dB に下げ、着地で 180ms かけて戻す。最後の板だけは白のまま倒れ、着地と同時に金へ開く
- グローは縮小キャンバス（1/6）を別 DOM レイヤーに置き、CSS の `mix-blend-mode: screen` + `blur(6px)` で合成。本体キャンバスへの加算は避けた（白飽和対策）
- 確定光跡は trail canvas（p5 キャンバスの下の DOM レイヤー）に不透明な金で焼き付け、最大 24 本。上限超過時だけ最古の 1 本を 1500ms でフェードする
- `place()` の発音は 84ms に間引くが、板の視覚配置は間引かない

## Phase 2: 検証（pnpm verify）

- `pnpm verify light-domino`: 全項目 pass。maxAmp 1280=0.0067 / 375=0.0071、meanDrawMs 1280=0.59 / 375=0.26
- 追加の実測プローブ（ヘッドレスの一時スクリプト。コミットしない）:
  - S 字カーブをなぞって端をタップすると、state が `aligned → chain → finale` まで進み、光跡が金色の連続した曲線として残る（x 方向の複数の桶にわたり y が滑らかに変化する金色画素を確認）
  - 375px 幅 + CDP タッチでも同じ操作が成立（finale 到達、maxAmp 0.148）
  - amp 推移: 連鎖中の加速に従って amp が上がり（0.018 → 0.13）、hush の 160ms で落ちて、finale で 0.098 → ピーク 0.153 の和音
  - 連鎖時間: 19 枚の列で約 6.7 秒（1.4〜9 秒の範囲内）。375px では約 8.2 秒

## Phase 3: 公開

draft PR で設計を引き渡し、実装後の人間による merge で公開する。

## Phase 4: コードレビュー修正

音声予約では `performance.now()` と `AudioContext.currentTime` を連鎖開始時に対応付け、スケジュール絶対値の加算ではなく開始時刻からの差分で AudioContext 時刻へ変換するよう修正した。あわせて 250ms 内の密度判定を実発音予定時刻基準へ変更し、同時発音の管理も予定区間の重なりで判定する。上限到達時は対象時刻の最古の通常音を新音の直前 20ms でフェードし、管理配列から即時除外することで、同じ音の重複 steal と 28 音超過を防ぐ。

リサイズ時は正規化された板中心を現在の画面寸法で px 座標へ戻してから接線を再計算し、連鎖中は既存 schedule と同じ板数・順序だけを採用するよう修正した。空列の端参照も防御し、trail canvas は DOM 要素を作り直さず同じ要素の寸法を更新して正規化データから再描画する。上限超過した光跡は fade 中の各フレームで静的レイヤーを塗り直し、完了時にデータと画素を同時に除去する。

整列済みの列では端以外の pointerdown を候補として保持し、10px 以上の移動で旧列を破棄して tracing を開始するよう修正した。10px 未満の誤タップは列を維持して端を明滅させる。連鎖音は `scale.ts` の degree から C4〜E6 の MIDI 列を生成し、タップした端を起点とする連鎖 index / count を進行率へ写すことで、どちらの端から開始しても C メジャーペンタ内を単調上昇するようにした。

## Phase 5: 再設計ラウンド「暗い展示室」（t_d8ddf15e が分解）

レビューの根拠 4 点（単調さ・物体感の不足・光の孤独・モバイル終演の空白）を消化する全面再設計。eng-lead の分解カード t_d8ddf15e が 7 枚（web-designer の独立アートディレクション案 → coder の正本更新 → coder の視覚全面再実装 → tester の検品 → web-designer / reviewer の判定 → eng-lead の統合）へ直列化した。

- **アートディレクション案**（web-designer、1fab6fe docs 追加）: 既存実装を見ずに「暗い展示室の床の上で、描線が光の楽器／星図に育つ」という世界観、4 色系統（墨・冷たい照り・温かい白・金）、床 4 層（L0〜L3）、板の物語性（立ち → 倒れ中 → 倒れた → 確定道＝星図）を決めた。合成の規律（金は alpha 1.0 で 1 回描き・グロー積算 alpha ≤ 0.12・混色は照り 1 層の内部のみ）と禁則（濁った茶色・白飛び・太い原色リング）を第 0 章・第 6 章に置いた
- **正本更新**（coder、a6ccbe4）: concept.md と architecture.md を art-direction に沿って書き直した。状態機械は 5 状態のまま、床の常駐描画・眠る光の断片・velocity の写像（§4.1）・確定道の星図を正本へ反映した
- **視覚全面再実装**（coder、99270f0 + 0540862）: 床の照り・格子・周辺減光・眠る光、板の疑似 3D（縁取り・手前側面・エッジ反射）、着地パルス、床の照りの連鎖追従と色温度の物語、道に沿う finale 開花、確定星図（蕊 + 節点 + 10s 残光）を実装。調整ノブを tuning.ts へ集約する方針もこのラウンドで導入した
- **検品**（tester、86febd6）: 6 状態 × 375/1280px の実入力キャプチャ（計 20 枚）で taste-guide 4 観点すべて Yes。白飛び 0・濁りは線の縁のみ・375px の主役画素比が全状態で 1280px 以上・intro の文字は埋もれず。サムネイルを finale 画からの cover 切り出しに更新
- **見た目判定**（web-designer、t_ff6e2c8c）: **verdict: passed**（veto 0 / must-fix 0 / note 2）。art-direction 全章の反映・taste-guide 10/10 Yes・レビュー根拠 4 軸すべて解消を 20 枚の実見と PIL 実測で確認。note は「commit 系の濁り近似面積が他状態より一桁大きい（交差点の落下影の集積で許容範囲）」と「intro 案内文の表記ゆれ程度」の 2 件
- **コードレビュー**（reviewer、t_4bc3a9f4）: **verdict: failed**（veto 0 / must-fix 4 / note 7）。must-fix は ①交差検出が始端×終端の交差を取りこぼす ②節点上限 300 超過時の挙動が正本 §7.2 と逆 ③床・格子・周辺減光・眠る光の静的焼き付けが未実施（毎フレーム再描画）④tuning.ts 集約違反（直書き定数・正本記載定数の欠落・蕊幅 2.5px が §7.2 の 1.5px から +67% 乖離）。note 7 件のうち「正本 architecture.md 自身の内部矛盾・古い記述」（note 3）は docs 側の修正カードへ分離した

## Phase 6: 修正ラウンド（must-fix 1〜4 + docs 整備、eng-lead t_98d7a24b が分解）

統合カード t_98d7a24b が reviewer 判定を digest し、修正 5 枚（修正 1→2→3→4→5 の直列）→ 再審査 t_cf3d3e21 → 後続統合 t_e08760f9 のチェーンを起票した。各修正の決定（正本に寄せるか実装に寄せるか）は eng-lead が下した。

- **修正 1**（t_00de73e7、21f4289）: MF-1。path.ts の交差検出から「first === 0 && second === points.length - 2 を skip」の誤った慣習行を削除し、始端×終端セグメントの交差（輪を描いて閉じるなぞり）も検出できるようにした。併せて満たない列の消滅を即時ではなく STROKE_DISCARD_MS（240ms）のフェードへ変更（reviewer note 2 の短列消滅に対応）
- **修正 2**（t_9a38411c、ed1ae60）: MF-2。節点上限 300 超過時の挙動を正本 §7.2 どおりへ変更。新しい順序は「上限超過時も節点・蕊は消さず、新しい演奏から 300 個ぶんを確定直後（蕊 alpha 1.0）、あふれた古い演奏から順に残光段階（蕊 alpha 0.5）へ減衰」。旧実装の `trails.length > 8 ? 0.8 : 1` という独自ルールを廃止し、TRAIL_CORE_ALPHA_COMMIT=1.0 / TRAIL_CORE_ALPHA_AFTERGLOW=0.5 を tuning.ts へ新設
- **修正 3**（t_68256316、8a14c13）: MF-3。床の静的層（L1 放射照り・L2 格子・周辺減光）と眠る光の円弧をオフスクリーンへ 1 回焼きに移行。リサイズ時だけ再焼きし、眠る光は焼いたスプライトの転画 + alpha 掛けのみにした（正本 §7.3・§3.1 の明文化へ実装側を寄せる判断）
- **修正 4**（t_2b075c34、a9c6838）: MF-4。main.ts の直書き数値を tuning.ts へ集約（新規 19 定数。照り色の RGB 直書きは PALETTE_* を import、モバイル判定は min(w,h) ≤ MOBILE_BREAKPOINT_PX=430 に統一）。蕊幅 TRAIL_WIDTH_PX は 2.5 → 1.5 へ変更 ─ 正本 §7.2 の 1.5px への値変更例外として eng-lead が承認した唯一の値変更
- **修正 5**（t_ab0cd892、本ラウンド）: reviewer note 3 の正本自体の内部矛盾・古い記述を実態に合わせる。docs の修正が先、コードは追従しない（docs は正本）。§7.3 の入力 clamp 12px → 24px（実装キー INPUT_EDGE_INSET_PX）、§3.4 の倒れイージング easeInCubic → easeInQuad（§7.2 には実装が cubic 相当・60 度切替の実測を補足）、終演スパークル上限の記述を削除（再設計でスパークル自体が削除済み。quality.ts の判定段は ?debug 診断表示の載せるだけ）、§3.5 の「確定の誘いを押すと」と 1200ms 後焼き付けの両立を settleAtMs 自動確定へ統一（誘いの押下は逆走再描画のみ、逆走は自動確定に同期したグロー層の走査演出）、§3.2 の仮板列 60ms 遅れ追従を即時追従へ、§3.3・§4 表の 220ms 吸着を即時吸着へ（ALIGN_MS は legacy キーとして保持）、§10 を実在定数の全量へ書き直し（legacy キーは削除せず項を分けて明記）
